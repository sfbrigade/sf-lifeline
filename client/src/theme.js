import { createTheme } from '@mantine/core';

export const theme = createTheme({
  //themes here
  fontFamily: 'sans-serif',
  defaultRadius: 'md',
  breakpoints: {
    xs: '30em',
    sm: '48em',
    md: '64em',
    lg: '74em',
    xl: '90em',
  },
});
