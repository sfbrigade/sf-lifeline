{
  "name": "sf-lifeline",
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "nf start -j Procfile.dev",
    "test": "npm run test --workspaces",
    "format": "npm run format --workspaces",
    "format:check": "npm run format --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "dependencies": {
    "foreman": "^3.0.1"
  }
}
